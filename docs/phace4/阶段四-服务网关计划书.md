# 📋 阶段四：服务网关（Spring Cloud Gateway）详细计划书

## 🎯 学习目标

完成本阶段后，你将能够：

1. **理解网关架构** - 明白为什么微服务需要网关层
2. **搭建 Gateway 服务** - 创建独立的网关模块
3. **配置路由规则** - 实现请求的动态转发
4. **实现全局鉴权** - 基于 JWT 的统一身份验证
5. **处理跨域问题** - 解决前后端分离的 CORS 问题
6. **配置限流熔断** - 保护后端服务

***

## 📚 核心概念

### 1. 什么是 API 网关？

```
┌─────────────────────────────────────────────────────────────┐
│                      客户端 (前端/移动端)                      │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Spring Cloud Gateway                      │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐  │
│  │   路由转发   │   身份验证   │   限流熔断   │   日志监控   │  │
│  └─────────────┴─────────────┴─────────────┴─────────────┘  │
└─────────────────────────────────────────────────────────────┘
         │              │              │
         ▼              ▼              ▼
   ┌──────────┐   ┌──────────┐   ┌──────────┐
   │  User    │   │  Course  │   │  其他服务  │
   │  :9001   │   │  :9002   │   │   ...    │
   └──────────┘   └──────────┘   └──────────┘
```

**网关的职责：**

- ✅ 统一入口 - 所有请求先经过网关
- ✅ 安全屏障 - 鉴权、防攻击
- ✅ 流量控制 - 限流、熔断
- ✅ 路由分发 - 转发到正确的服务

### 2. Gateway 三大核心概念

| 概念      | 英文        | 作用       | 举例                                               |
| ------- | --------- | -------- | ------------------------------------------------ |
| **路由**  | Route     | 定义请求如何转发 | `/api/user/**` → `http://yunh-service-user:9001` |
| **断言**  | Predicate | 匹配请求的条件  | Path、Method、Header、Time                          |
| **过滤器** | Filter    | 处理请求/响应  | 鉴权、日志、跨域、限流                                      |

### 3. 过滤器类型

```
┌─────────────────────────────────────────────────────────────┐
│                     Global Filter (全局)                     │
│  对所有路由生效，如：JWT 鉴权、统一日志、跨域处理              │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│               GatewayFilter (局部/路由级)                    │
│  只对特定路由生效，如：某个服务的特殊处理                     │
└─────────────────────────────────────────────────────────────┘
```

***

## 📁 模块结构

### 创建新模块：`yunh-gateway`

```
yunh-cloud/
├── yunh-gateway/                 # ⭐ 新增网关模块 (端口 9000)
│   ├── src/main/java/
│   │   └── com/yunh/gateway/
│   │       ├── GatewayApplication.java      # 启动类
│   │       ├── config/
│   │       │   ├── CorsConfig.java          # 跨域配置
│   │       │   └── SwaggerConfig.java       # API 文档（可选）
│   │       ├── filter/
│   │       │   ├── AuthGlobalFilter.java    # JWT 鉴权过滤器
│   │       │   └── LogGlobalFilter.java     # 日志记录过滤器
│   │       └── handler/
│   │           └── GlobalExceptionHandler.java # 全局异常处理
│   ├── src/main/resources/
│   │   ├── bootstrap.yml                    # Nacos 配置
│   │   └── application.yml                  # 应用配置
│   └── pom.xml
├── yunh-service/
│   ├── yunh-service-user/
│   └── yunh-service-course/
└── ...
```

***

## 🔧 实施步骤

### 步骤 4.1：创建 Gateway 模块（预计 15 分钟）

#### 4.1.1 在父 pom.xml 中注册模块

```xml
<modules>
    <module>yunh-basic</module>
    <module>yunh-pojo</module>
    <module>yunh-api</module>
    <module>yunh-service</module>
    <module>yunh-support</module>
    <module>yunh-gateway</module>  <!-- 新增 -->
</modules>
```

#### 4.1.2 创建 pom.xml

```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    
    <parent>
        <groupId>com.yunh</groupId>
        <artifactId>yunh-cloud</artifactId>
        <version>1.0.0</version>
    </parent>
    
    <artifactId>yunh-gateway</artifactId>
    <name>云课堂网关服务</name>
    
    <dependencies>
        <!-- Spring Cloud Gateway -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-gateway</artifactId>
        </dependency>
        
        <!-- Nacos Discovery -->
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
        </dependency>
        
        <!-- Nacos Config -->
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-config</artifactId>
        </dependency>
        
        <!-- JWT 工具（可选：复用 yunh-basic 中的工具类）-->
        <dependency>
            <groupId>com.yunh</groupId>
            <artifactId>yunh-basic</artifactId>
            <version>1.0.0</version>
        </dependency>
        
        <!-- Actuator 监控 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        
        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
    </dependencies>
    
    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
```

#### 4.1.3 创建启动类

```java
package com.yunh.gateway;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.cloud.client.discovery.EnableDiscoveryClient;

@SpringBootApplication
@EnableDiscoveryClient
public class GatewayApplication {
    public static void main(String[] args) {
        SpringApplication.run(GatewayApplication.class, args);
    }
}
```

***

### 步骤 4.2：配置路由规则（预计 20 分钟）

#### 4.2.1 创建 bootstrap.yml

```yaml
server:
  port: 9000  # 网关端口

spring:
  application:
    name: yunh-gateway
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        username: your_nacos_username
        password: your_password_here
      config:
        server-addr: 127.0.0.1:8848
        username: your_nacos_username
        password: your_password_here
        file-extension: yml
        # 共享配置（可选）
        shared-configs:
          - data-id: yunh-gateway.yml
            refresh: true
```

#### 4.2.2 在 Nacos 控制台配置路由（推荐方式）

**Data ID**: `yunh-gateway.yml`\
**Group**: `DEFAULT_GROUP`\
**配置格式**: `YAML`

```yaml
spring:
  cloud:
    gateway:
      routes:
        # 用户服务路由
        - id: yunh-service-user
          uri: lb://yunh-service-user  # lb 表示负载均衡
          predicates:
            - Path=/api/user/**
          filters:
            - StripPrefix=1  # 去掉路径中的第 1 层前缀
            - name: Retry
              args:
                retries: 3
                statuses: BAD_GATEWAY
        
        # 课程服务路由
        - id: yunh-service-course
          uri: lb://yunh-service-course
          predicates:
            - Path=/api/course/**
          filters:
            - StripPrefix=1
        
        # 网关自身接口（如健康检查）
        - id: yunh-gateway-actuator
          uri: lb://yunh-gateway
          predicates:
            - Path=/actuator/**
```

**路由说明：**

| 配置项          | 说明     | 示例                      |
| ------------ | ------ | ----------------------- |
| `id`         | 路由唯一标识 | `yunh-service-user`     |
| `uri`        | 目标服务地址 | `lb://服务名` 表示从 Nacos 发现 |
| `predicates` | 匹配条件   | `Path=/api/user/**`     |
| `filters`    | 过滤器链   | `StripPrefix=1` 去掉前缀    |

**请求转发示例：**

```
客户端请求：http://localhost:9000/api/user/1
          ↓
网关匹配：Path=/api/user/** → 路由到 yunh-service-user
          ↓
StripPrefix=1: 去掉 /api → /user/1
          ↓
最终转发：http://yunh-service-user:9001/user/1
```

***

### 步骤 4.3：实现全局鉴权过滤器（预计 30 分钟）

#### 4.3.1 创建 JWT 鉴权过滤器

```java
package com.yunh.gateway.filter;

import com.yunh.basic.util.JwtUtil;
import lombok.extern.slf4j.Slf4j;
import org.springframework.cloud.gateway.filter.GatewayFilterChain;
import org.springframework.cloud.gateway.filter.GlobalFilter;
import org.springframework.core.Ordered;
import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.server.reactive.ServerHttpRequest;
import org.springframework.http.server.reactive.ServerHttpResponse;
import org.springframework.stereotype.Component;
import org.springframework.util.AntPathMatcher;
import org.springframework.web.server.ServerWebExchange;
import reactor.core.publisher.Mono;

import java.util.List;

@Slf4j
@Component
public class AuthGlobalFilter implements GlobalFilter, Ordered {
    
    // 放行的白名单路径
    private static final List<String> WHITE_LIST = List.of(
        "/api/user/login",      // 登录接口
        "/api/user/register",   // 注册接口
        "/actuator/**",         // 健康检查
        "/favicon.ico"          // 图标
    );
    
    private final AntPathMatcher pathMatcher = new AntPathMatcher();
    
    @Override
    public Mono<Void> filter(ServerWebExchange exchange, GatewayFilterChain chain) {
        ServerHttpRequest request = exchange.getRequest();
        String path = request.getPath().value();
        
        // 1. 检查是否在白名单中
        if (isWhiteList(path)) {
            log.info("[放行] 白名单路径：{}", path);
            return chain.filter(exchange);
        }
        
        // 2. 从请求头获取 JWT Token
        String token = request.getHeaders().getFirst(HttpHeaders.AUTHORIZATION);
        
        // 3. Token 为空
        if (token == null || token.isEmpty()) {
            log.warn("[拦截] 缺少 Token: {}", path);
            return unauthorized(exchange, "未授权：缺少 Token");
        }
        
        // 4. 处理 Bearer 前缀
        if (token.startsWith("Bearer ")) {
            token = token.substring(7);
        }
        
        // 5. 验证 Token
        try {
            // TODO: 使用 JwtUtil 验证 Token
            // JwtUtil.verifyToken(token);
            log.info("[通过] Token 验证成功：{}", path);
            
            // 6. 可选：将用户信息传递给下游服务
            // exchange.getRequest().mutate()
            //     .header("X-User-Id", userId)
            //     .build();
            
        } catch (Exception e) {
            log.error("[拦截] Token 无效：{} - {}", path, e.getMessage());
            return unauthorized(exchange, "Token 无效或已过期");
        }
        
        return chain.filter(exchange);
    }
    
    /**
     * 检查是否在白名单中
     */
    private boolean isWhiteList(String path) {
        return WHITE_LIST.stream()
            .anyMatch(pattern -> pathMatcher.match(pattern, path));
    }
    
    /**
     * 返回 401 未授权
     */
    private Mono<Void> unauthorized(ServerWebExchange exchange, String message) {
        ServerHttpResponse response = exchange.getResponse();
        response.setStatusCode(HttpStatus.UNAUTHORIZED);
        response.getHeaders().add("Content-Type", "application/json;charset=UTF-8");
        
        String body = String.format("{\"code\":401,\"message\":\"%s\"}", message);
        return response.writeWith(
            Mono.just(response.bufferFactory().wrap(body.getBytes()))
        );
    }
    
    @Override
    public int getOrder() {
        return -100; // 高优先级，优先执行
    }
}
```

***

### 步骤 4.4：配置跨域处理（预计 10 分钟）

#### 4.4.1 创建跨域配置类

```java
package com.yunh.gateway.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.web.cors.CorsConfiguration;
import org.springframework.web.cors.reactive.CorsWebFilter;
import org.springframework.web.cors.reactive.UrlBasedCorsConfigurationSource;
import org.springframework.web.util.pattern.PathPatternParser;

@Configuration
public class CorsConfig {
    
    @Bean
    public CorsWebFilter corsWebFilter() {
        CorsConfiguration config = new CorsConfiguration();
        
        // 允许的来源
        config.addAllowedOriginPattern("*"); // 生产环境建议指定具体域名
        
        // 允许的方法
        config.addAllowedMethod("GET");
        config.addAllowedMethod("POST");
        config.addAllowedMethod("PUT");
        config.addAllowedMethod("DELETE");
        config.addAllowedMethod("OPTIONS");
        
        // 允许的请求头
        config.addAllowedHeader("*");
        
        // 允许携带凭证（Cookie、Authorization 等）
        config.setAllowCredentials(true);
        
        // 预检请求缓存时间（秒）
        config.setMaxAge(3600L);
        
        UrlBasedCorsConfigurationSource source = new UrlBasedCorsConfigurationSource(
            new PathPatternParser()
        );
        source.registerCorsConfiguration("/**", config);
        
        return new CorsWebFilter(source);
    }
}
```

***

### 步骤 4.5：日志记录过滤器（预计 15 分钟）

#### 4.5.1 创建日志过滤器

```java
package com.yunh.gateway.filter;

import lombok.extern.slf4j.Slf4j;
import org.springframework.cloud.gateway.filter.GatewayFilterChain;
import org.springframework.cloud.gateway.filter.GlobalFilter;
import org.springframework.core.Ordered;
import org.springframework.http.HttpHeaders;
import org.springframework.http.server.reactive.ServerHttpRequest;
import org.springframework.stereotype.Component;
import org.springframework.web.server.ServerWebExchange;
import reactor.core.publisher.Mono;

import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;

@Slf4j
@Component
public class LogGlobalFilter implements GlobalFilter, Ordered {
    
    private static final DateTimeFormatter formatter = 
        DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss.SSS");
    
    @Override
    public Mono<Void> filter(ServerWebExchange exchange, GatewayFilterChain chain) {
        ServerHttpRequest request = exchange.getRequest();
        
        // 记录请求开始时间
        long startTime = System.currentTimeMillis();
        String requestId = java.util.UUID.randomUUID().toString().substring(0, 8);
        
        // 记录请求信息
        log.info("[{}] 请求开始 | 时间：{} | 方法：{} | 路径：{} | IP: {}",
            requestId,
            LocalDateTime.now().format(formatter),
            request.getMethod(),
            request.getPath(),
            getClientIp(request)
        );
        
        // 记录请求头（可选，生产环境注意脱敏）
        HttpHeaders headers = request.getHeaders();
        log.debug("[{}] 请求头：{}", requestId, headers);
        
        return chain.filter(exchange).then(Mono.fromRunnable(() -> {
            // 记录响应信息
            long duration = System.currentTimeMillis() - startTime;
            log.info("[{}] 请求结束 | 耗时：{}ms | 状态码：{}",
                requestId,
                duration,
                exchange.getResponse().getStatusCode()
            );
        }));
    }
    
    private String getClientIp(ServerHttpRequest request) {
        String xForwardedFor = request.getHeaders().getFirst("X-Forwarded-For");
        if (xForwardedFor != null && !xForwardedFor.isEmpty()) {
            return xForwardedFor.split(",")[0].trim();
        }
        return request.getRemoteAddress() != null 
            ? request.getRemoteAddress().getAddress().getHostAddress()
            : "unknown";
    }
    
    @Override
    public int getOrder() {
        return -200; // 最高优先级，在鉴权之前执行
    }
}
```

***

## 🧪 测试验证

### 测试清单

| 测试项      | 测试方法                                        | 预期结果                 |
| -------- | ------------------------------------------- | -------------------- |
| 网关启动     | 访问 `http://localhost:9000/actuator/health`  | 返回 `{"status":"UP"}` |
| 服务注册     | Nacos 控制台查看服务列表                             | `yunh-gateway` 已注册   |
| 路由转发     | `GET http://localhost:9000/api/user/1`      | 转发到 User 服务          |
| 白名单放行    | `POST http://localhost:9000/api/user/login` | 无需 Token             |
| Token 验证 | 无 Token 访问受保护接口                             | 返回 401               |
| Token 无效 | 无效 Token 访问受保护接口                            | 返回 401               |
| Token 有效 | 有效 Token 访问受保护接口                            | 正常返回                 |
| 跨域处理     | 前端页面调用网关接口                                  | 无 CORS 错误            |

### 测试命令

```
测试1：健康检查
  方法：GET
  URL：http://localhost:9000/actuator/health
  预期：{"status":"UP"}

测试2：白名单接口（无需 Token）
  方法：POST
  URL：http://localhost:9000/api/user/login
  Headers：Content-Type: application/json
  Body (raw/JSON)：{"username":"test","password":"123456"}
  预期：登录成功（需先实现 login 接口）

测试3：受保护接口（无 Token）
  方法：GET
  URL：http://localhost:9000/api/user/1
  预期：{"code":401,"message":"未授权：缺少 Token"}

测试4：受保护接口（有 Token）
  方法：GET
  URL：http://localhost:9000/api/user/1
  Headers：
    Authorization: Bearer <your-jwt-token>
  预期：正常返回用户信息
```

***

## 📝 交付物清单

- [ ] `yunh-gateway` 模块创建完成
- [ ] `pom.xml` 依赖配置完成
- [ ] `GatewayApplication.java` 启动类
- [ ] `bootstrap.yml` Nacos 配置
- [ ] Nacos 配置 `yunh-gateway.yml`（路由规则）
- [ ] `AuthGlobalFilter.java` JWT 鉴权过滤器
- [ ] `LogGlobalFilter.java` 日志过滤器
- [ ] `CorsConfig.java` 跨域配置
- [ ] 所有测试用例通过

***

## ⚠️ 注意事项

### 1. 依赖版本

确保 `spring-cloud-starter-gateway` 版本与 Spring Cloud 版本兼容

### 2. 服务发现

Gateway 需要配置 Nacos Discovery 才能使用 `lb://` 负载均衡

### 3. Token 传递

如果下游服务需要用户信息，通过请求头传递：

```java
request.mutate().header("X-User-Id", userId).build();
```

### 4. 生产环境

- 白名单路径需要根据实际业务调整
- CORS 配置不要使用 `*`，指定具体域名
- 添加限流过滤器保护后端服务
- 配置熔断降级策略

***

## 📚 扩展学习

### 后续可选功能

| 功能     | 说明                 | 优先级 |
| ------ | ------------------ | --- |
| 限流     | 基于 Redis 的令牌桶限流    | ⭐⭐⭐ |
| 熔断降级   | Sentinel 集成        | ⭐⭐⭐ |
| API 文档 | Gateway 聚合 Swagger | ⭐⭐  |
| 灰度发布   | 基于 Header 的路由      | ⭐⭐  |
| 请求缓存   | 响应缓存过滤器            | ⭐   |

***

**预计总耗时：** 约 90 分钟\
**难度等级：** ⭐⭐⭐ 中等

***

*文档创建时间：2026-04-04*\
*适用项目：云课堂 (YunH) Spring Cloud 微服务学习项目*
