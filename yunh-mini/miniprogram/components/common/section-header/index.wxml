<view class="section-header {{compact ? 'compact' : ''}}">
  <view class="header-main">
    <view wx:if="{{eyebrow}}" class="eyebrow">{{eyebrow}}</view>
    <view class="title">{{title}}</view>
    <view wx:if="{{desc}}" class="desc">{{desc}}</view>
  </view>
  <slot name="extra"></slot>
</view>
